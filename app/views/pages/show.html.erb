
<h2>Page Preview - <%= @page.title %></h2>
<div class='<%= @page.style %>'>
	<%= render html: @page.body.html_safe %>
</div>
<p id="notice"><%= notice %></p>

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
